# 🎮 새 게임 추가하는 법

새로 만든 게임을 이 사이트에 올리는 방법이에요. **순서대로 4단계**만 하면 됩니다.

> 이 문서의 목적은 **누가 하든 똑같은 결과가 나오게** 하는 거예요.
> 특히 썸네일은 반드시 3단계의 스크립트로 만들어 주세요. 손으로 찍은 캡처는 크기·비율·화질이 제각각이라 카드가 어긋나 보입니다.

---

## 1단계. 게임 파일 넣기

만든 HTML 파일을 아래 두 폴더 중 한 곳에 넣습니다.

| 폴더 | 무엇을 넣나요 |
| --- | --- |
| `games/` | **아이들이 만든 게임** |
| `examples/` | **완성 예시 게임** (아이들이 따라 만들 견본) |

### 파일 이름 규칙

- **영어 소문자 + 하이픈(-)** 만 씁니다. → `my-new-game.html`
- 한글, 띄어쓰기, 괄호, 대문자는 쓰지 마세요.
  GitHub Pages는 리눅스에서 돌아가서 **대소문자를 구분**하고, 한글·공백은 주소에서 깨집니다.
- ❌ `내 게임 (1).html` · `MyGame.html` · `code_artifact (7).html`
- ⭕ `my-new-game.html` · `gold-miner.html`

### 게임 파일이 지켜야 할 것

- **HTML 파일 하나**로 완결되어야 합니다. (CSS·JS를 파일 안에)
- `localStorage` 를 쓴다면 **반드시 `try` / `catch` 로 감싸세요.**
  카드 미리보기는 게임을 `iframe` 안에서 돌리는데, 여기서 예외가 나면 **게임 전체가 안 뜹니다.**
  ```js
  let best = 0;
  try { best = Number(localStorage.getItem('my_game_best') || 0); } catch (e) {}
  ```
- 시작 화면을 넣어 주세요. (없어도 되지만 있으면 훨씬 보기 좋습니다)
- 키보드와 마우스/터치를 **둘 다** 지원해 주세요.

---

## 2단계. `index.html` 목록에 한 줄 추가

`index.html` 을 열고 아래 배열 중 맞는 곳을 찾아 한 줄 추가합니다.

| 배열 이름 | 어디에 보이나요 |
| --- | --- |
| `GAMES` | 🕹️ 게임 저장소 탭 |
| `NEWGAMES` | ✨ 새 게임 탭 |
| `CLASSICS` | 💬 프롬프트 저장소 탭 › 고전 명작 |
| `IDEAS` | 💬 프롬프트 저장소 탭 › 완성 예시 |

### `GAMES` / `NEWGAMES` 에 넣을 때

```js
{f:'my-new-game.html', t:'내가 만든 새 게임', d:'하늘에서 별을 받아요', c:'← → 이동 · 스페이스 시작', k:'2d', g:'b', e:'🎈'}
```

| 항목 | 뜻 | 예시 |
| --- | --- | --- |
| `f` | **f**ile — `games/` 안의 파일 이름 | `'my-new-game.html'` |
| `t` | **t**itle — 카드 제목 | `'내가 만든 새 게임'` |
| `d` | **d**escription — 한 줄 설명 (24자 이내) | `'하늘에서 별을 받아요'` |
| `c` | **c**ontrols — 조작법 한 줄 | `'← → 이동 · 스페이스 시작'` |
| `k` | **k**ind — `'2d'` / `'3d'` / `'canvas'` | `'2d'` |
| `g` | **g**roup — 묶음 `'a'` 또는 `'b'` (`GAMES` 만) | `'b'` |
| `e` | **e**moji — 제목 앞 이모지 하나 | `'🎈'` |
| `by` | **만든이 이름** (선택) — 넣으면 썸네일 왼쪽 아래에 표시됩니다 | `'한0민'` |

> 만든이 이름은 **가운데 글자를 `0` 으로 가려서** 적어 주세요. (`한0민`, `김0수`)
> 학생 실명을 인터넷에 그대로 올리지 않기 위한 규칙입니다.

`k` 고르는 법: **three.js를 쓰면** `'3d'`, **`<canvas>`에 직접 그리면** `'canvas'`, **화면 요소(div 등)를 움직이면** `'2d'`.

### `IDEAS` / `CLASSICS` 에 넣을 때

이쪽은 `slug` 를 씁니다. 파일이 `examples/my-example.html` 이면 `slug` 는 `'my-example'` 입니다.

```js
{
  "slug": "my-example",
  "ico": "🎈", "bg": "#FFD84D",
  "t": "게임 이름", "d": "한 줄 설명",
  "lv": 2,                          // 난이도 1~3
  "ctrl": "← → 이동 · 스페이스 시작",
  "howto": "짧은 소개 (26자 이내)",
  "steps": [                        // 아이가 따라 만들 프롬프트 5~6단계
    { "t": "화면 만들기", "s": "화면부터 예쁘게",
      "p": "HTML 파일 하나로 되는 게임을 만들 거야. 지금은 화면만 만들어줘.\n- ...\n코드는 파일 하나(index.html)에 전부 넣어줘.",
      "c": "무엇이 보이면 성공인지" }
  ]
}
```

> `steps` 의 **2단계부터는 반드시 `지금 만든 게임에서,` 로 시작**해야 합니다.
> 이게 없으면 AI가 처음부터 새로 만들어서 앞에서 만든 게 사라져요. 수업의 핵심 규칙입니다.

### 화면 위 숫자도 같이 고쳐 주세요

게임을 추가하면 히어로의 숫자와 탭 옆 숫자가 안 맞게 됩니다.

- `<div class="stat"><b>21</b><span>아이들이 만든 게임</span></div>`
- `<button ... data-tab="games">🕹️ 게임 저장소<span class="n">21</span></button>`

---

## 3단계. 썸네일 굽기 ⭐ **가장 중요**

카드에 보이는 그림은 **게임을 진짜로 실행해서 찍은 화면**입니다. 손으로 찍지 말고 아래 스크립트를 쓰세요.

### 방금 추가한 게임만 찍기

```powershell
.\tools\capture-thumbnails.ps1 -Only my-new-game
```

여러 개를 한 번에 찍을 때는 **PowerShell 창 안에서** 배열로 넘기세요.

```powershell
.\tools\capture-thumbnails.ps1 -Only @('game-one','game-two') -Dir games
```

> ⚠️ `powershell -File ...` 형태로 밖에서 실행하면 `-Only a,b` 가 **문자열 하나로** 넘어가서
> "찍을 게임이 없습니다" 가 뜹니다. 여러 개를 찍을 땐 PowerShell 창을 열고 위처럼 `@( )` 로 주세요.

### 전체 다시 찍기

```powershell
.\tools\capture-thumbnails.ps1
```

찍힌 파일은 `assets/shots/<폴더>__<파일이름>.jpg` 로 저장됩니다.
예: `games/my-new-game.html` → `assets/shots/games__my-new-game.jpg`

**이름 규칙이 어긋나면 카드에 그림이 안 나옵니다.** 스크립트를 쓰면 자동으로 맞습니다.

### 이 스크립트가 하는 일

1. Windows에 원래 깔려 있는 **Microsoft Edge를 헤드리스로** 실행합니다. (Node·Python·Playwright 설치 필요 없음)
2. **원본 게임 파일은 건드리지 않습니다.** 임시 폴더에 사본을 만들고, 그 사본에만 `tools/auto-start.html` 을 끼워 넣습니다.
3. 끼워 넣은 스크립트가 게임을 **자동으로 시작**시킵니다.
   - `시작 / 스타트 / start / 플레이 / 모드 / ▶` 가 들어간 버튼을 찾아 실제로 클릭
   - 스페이스·엔터 키 입력 (옛날 코드가 쓰는 `keyCode` 까지 채워서)
   - 캔버스·화면 중앙 탭
4. 잠깐 **봇처럼 플레이**합니다. (방향키 좌우 / 스페이스 / 마우스 이동 / 작은 대상 클릭)
5. **촬영 1.5초 전에 조작을 멈춥니다.** → "헛스윙!" 같은 일시적인 글자가 사라진 뒤 찍혀서 화면이 깔끔합니다.
6. 1280×800 으로 찍어 **640×400 JPEG(품질 78)** 로 줄입니다. 장당 12~35KB 정도.

### 결과가 마음에 안 들 때

| 상황 | 해결 |
| --- | --- |
| 시작 화면 그대로 찍고 싶다 | `-NoAutoStart` 를 붙이세요 |
| 게임이 시작되기 전에 찍혔다 | `-Budget 8000` 처럼 시간을 늘리세요 (기본 5000) |
| 게임 오버 화면이 찍혔다 | `-Budget 3000` 처럼 줄이세요 |
| 화질을 더 높이고 싶다 | `-Quality 88` (파일이 커집니다) |
| 시작 버튼을 못 찾는다 | `tools/auto-start.html` 의 `var RX = /시작\|스타트\|.../i` 에 버튼 글자를 추가하세요 |

```powershell
# 예: 8초까지 기다렸다가 찍기
.\tools\capture-thumbnails.ps1 -Only my-new-game -Budget 8000
```

---

## 4단계. 올리기

```powershell
git add -A
git commit -m "새 게임 추가: 내가 만든 새 게임"
git push
```

1~2분 뒤 https://kwonjungu.github.io/ban12/ 에 반영됩니다.
안 보이면 **Ctrl + Shift + R** 로 브라우저 캐시를 비우세요.

---

## 확인 체크리스트

올리기 전에 훑어보세요.

- [ ] 파일 이름이 **영어 소문자 + 하이픈** 인가요?
- [ ] `index.html` 배열에 한 줄 넣었나요?
- [ ] `assets/shots/` 에 썸네일이 생겼나요? (**스크립트로** 만들었나요?)
- [ ] 히어로·탭의 게임 개수 숫자를 고쳤나요?
- [ ] 게임을 브라우저에서 직접 열어 봤나요?
- [ ] `localStorage` 를 `try` / `catch` 로 감쌌나요?

---

## 참고 — 폴더 구조

```
bangok-vibe-coding/
├── index.html              ← 메인 페이지 (게임 목록도 여기 안에)
├── ADDING-GAMES.md         ← 지금 읽고 있는 파일
├── README.md
├── .nojekyll               ← 지우지 마세요 (GitHub Pages 설정)
├── games/                  ← 아이들이 만든 게임
├── examples/               ← 완성 예시 게임
├── assets/
│   └── shots/              ← 썸네일 (스크립트가 자동 생성)
└── tools/
    ├── capture-thumbnails.ps1   ← 썸네일 굽는 스크립트
    └── auto-start.html          ← 게임 자동 시작 주입 스크립트
```
